# AI0506 Reminders — 前端界面规格

> 本文件定义本项目的前端界面、交互与实现要求。
>
> 它描述的是“这个产品的前端应该如何表现”，不是具体代码实现文档。
> 业务逻辑、数据结构、权限、安全与后端规则以 `REMINDERS_PLAN.md`、Calendar 后端文档和实际代码为准。
>
> 本项目是 **SwiftUI / iPadOS 原生应用**，不是 Web 应用。原模板中属于浏览器与 DOM 的条目已改写为 SwiftUI / UIKit 对应约束。

---

# 0. 使用方式

规则分为三类：

- **必须**：默认视为验收条件。除非在第 22 节“已知例外与技术债”中明确记录，否则必须遵守。
- **建议**：默认采用，但可以根据实际需求修改。
- **项目决定**：本项目已在下文给出结论。

如果规范与实际产品需求冲突，不应默默忽略规范。应修改本文件，使规范与最终决定保持一致。

---

# 1. 项目与界面背景

## 1.1 基本信息

**项目名称：**
AI0506 Reminders（bundle id `com.ai0506.reminders`，显示名「AI0506 Reminders」，界面语言为简体中文）

**产品用途：**
在 iPad 上查看、提醒和管理 AI0506 Calendar 后端中的 Deadline（截止事项），提供比 Web 日历更安静、更适合触控的专用界面。

**主要用户：**
AI0506 Calendar 的单一本人用户（学生 / 研究者），在自己的 11 英寸 iPad Pro 上使用。不是多用户产品，没有协作、分享与社交界面。

**主要使用场景：**

- 在 iPad 上快速确认「今天要交什么」「有没有逾期」。
- 临时想起一件事，用自然语言或表单在几秒内建一条 Deadline。
- 做完一件事，立刻打勾完成。
- 不打开 App 时，通过主屏 Widget 和本地通知获知临近的截止事项。

技术边界：只读写 Calendar Deadline API，**不使用 EventKit**，不访问 Apple 日历或 Apple 提醒事项数据。

---

## 1.2 核心任务

用户进入产品后最主要要完成的事情：

1. 查看今天 / 即将到期 / 已逾期的截止事项，并判断轻重缓急。
2. 完成（或重新打开）一条截止事项。
3. 创建一条新的截止事项（表单创建或 AI 草稿创建）。

其中最重要、最高频的任务是：

> 打开 App 后，一眼看清接下来要做什么，并把已经做完的打勾。

前端设计应优先服务这个任务：列表的标题与截止时间是最高视觉层级，完成操作必须始终触手可及，不得被筛选、设置或 AI 入口挤占。

---

## 1.3 核心界面

**主要界面：**

> 三栏 Deadline 工作区：侧栏（视图与筛选）→ 中栏（按日期分组的 Deadline 列表）→ 详情栏（选中 Deadline）。
> 实现位置：`Reminders/Views/ContentView.swift` 的 `NavigationSplitView`。

**次要界面：**

- 新建截止事项表单（`DeadlineEditorSheet`，模态 sheet）
- AI 创建工作区（`AIComposerSheet`，模态 sheet）
- 主屏 Widget（`Widgets/DeadlineWidget.swift`，small / medium）

**低频管理界面：**

- 设置（`AppSettingsSheet`：Calendar 连接、访问令牌、截止事项提醒开关）

设置入口固定在侧栏底部 `safeAreaInset` 的齿轮按钮内，不得提升到中栏顶部操作区或详情页，与创建、AI、完成争夺同级空间。

---

## 1.4 最不能被打断的事情

用户进行以下行为时，界面更新（后台刷新、Widget 刷新、通知路由、网络恢复）不得打断操作：

- 在 AI 工作区输入自然语言（`TextEditor`）。
- 在创建表单里填写标题、备注、标签。
- 正在操作 `DatePicker` 选择截止日期或时间。
- 正在滚动 Deadline 列表或详情。
- 正在打开的 sheet（创建 / AI / 设置）——后台刷新不得关闭 sheet。
- 完成 / 重开的乐观更新动画进行中。

---

# 2. 核心体验原则

## 2.1 界面稳定

**必须**

状态变化不得意外改变无关界面元素的位置、尺寸或滚动位置。

本项目的具体含义：

- 切换 今天 / 即将到期 / 已逾期 / 全部 / 分类 / 学科 / 标签 时，侧栏与中栏顶部标题条位置不变，只有列表内容替换。
- 中栏顶部标题条固定高度 62pt；同步状态文字（`syncNote`）变化或消失不得让标题、AI 按钮、新建按钮位移。
- 完成 / 重开某条 Deadline 时，同组其他行不得因为该行高度变化而跳动。
- 后台 `refresh()` 返回新数据时，不得把用户正在看的位置弹回顶部。
- 错误提示不得把主要按钮向下推（错误通过 alert 或固定槽位呈现）。

允许发生几何变化的情况，是几何变化本身就是这次操作的预期结果：

- 用户主动打开 / 关闭 sheet；
- 竖屏下用户主动展开或收起侧栏；
- 用户主动新增一条 Deadline 导致列表多一行；
- AI 解析成功后草稿区出现（这是用户点击「分析这段话」的预期结果）。

## 2.2 内容变化不应无必要地成为布局变化

**建议**

容易变化的内容应尽可能在稳定的空间中变化。本项目已采用并应继续遵守的做法：

- 同步状态文字使用 `Text(store.syncNote ?? " ")` + `.opacity`，保留行高而不是让行消失。
- 创建表单里「全天」开启时用 `Color.clear.frame(height: 38)` 占住时间 `DatePicker` 的位置，避免 Section 高度跳变。
- AI 草稿区标题行固定 `frame(height: 28)`，置信度文字变化不推动下方字段。
- 列表行标题与副信息统一 `lineLimit(1)`，长标题截断而不是把行撑高。
- 详情页正文以 `frame(maxWidth: 560)` 限宽，长标题换行但不改变卡片结构。
- 按钮在 loading 时只改文案（「创建」→「正在创建…」），不改变按钮所在工具栏位置。

不得为了「不动」而把真实内容裁切掉：详情页备注、AI 草稿备注允许自然增高。

## 2.3 保留用户上下文

**必须**

刷新、异步请求、Widget/通知深链接、筛选切换不得无故破坏用户当前上下文。

除非用户明确执行了重置操作，否则应保留：

- AI 工作区已输入的自然语言与已解析草稿；
- 创建表单未提交的字段；
- 当前选中的 Deadline（`selectedDeadlineID`）；
- 当前筛选（`selectedFilter`）；
- 列表滚动位置；
- 打开的 sheet 与键盘焦点。

例外（已定义的合法重置）：

- 切换筛选后，如果当前选中项不在新视图中，`reconcileSelectionWithFilter()` 会改选该视图的第一项——这是预期行为；如果仍在视图中，必须保持原选中项。
- 通过 Widget 或通知深链接进入时，允许把筛选切到「全部截止事项」并选中目标 Deadline；数据尚未到达时先记 `pendingDeadlineID`，刷新后再选中，不得中途抢走用户已有选择之外的其他状态。

## 2.4 信息层级优先于装饰

**必须**

本项目的信息优先级：

1. Deadline 标题 + 截止时间（最高）
2. 状态：进行中 / 已逾期 / 已完成，以及完成操作
3. 优先级、分类色点
4. 学科、标签、备注
5. 同步状态、连接状态、设置入口（最低）

低优先级内容不得因为数量增加而挤掉核心信息：标签再多，列表行也只显示一行摘要并截断；分类 / 学科 / 标签在侧栏内部滚动，不得把「今天 / 即将到期 / 已逾期 / 全部」挤出可视区域。

## 2.5 新功能不能自动获得主界面空间

**建议**

新增功能先判断使用频率、重要程度、是否与核心任务直接相关、是否需要持续可见。

低频或复杂功能优先放入：sheet、侧栏底部、详情页内的次级区域、上下文菜单（`contextMenu`）、工具栏「更多」菜单。

本项目已生效的判断：Calendar 连接与通知设置是低频配置，放在设置 sheet；AI 是中频但非必选，放在中栏顶部的图标按钮 + 独立 sheet，不占据常驻输入区。

---

# 3. 视觉方向

## 3.1 总体视觉

本项目的视觉关键词：

- 纸与墨（中性灰阶、低饱和）
- 安静、克制、留白充足
- 原生 iPadOS 质感（圆角、柔和层次、系统语义色）

参考方向：

> Apple Reminders / Apple Calendar 的信息密度与克制感；配色与稳定性规则沿用 AI0506 Calendar 的 `FRONTEND_SPEC.md` 与 ChatAI 的稳定交互经验。

不希望出现的视觉倾向：

- 强烈品牌色、彩色渐变、玻璃拟态堆叠、重阴影。
- 用大色块或强调色表达「App 个性」；颜色只用于表达分类与状态数据。
- 花哨动效、弹跳、hover 缩放。

## 3.2 设计令牌

**必须**

颜色令牌集中定义在 `Reminders/Theme/DesignSystem.swift` 的 `RemindersTheme`，业务视图不得重复写死同类视觉值：

```text
paper             背景      .systemGroupedBackground
card              表面      .secondarySystemGroupedBackground
pale              弱表面    .tertiarySystemGroupedBackground
ink               主文字    .label
muted             次要文字  .secondaryLabel
actionForeground  反色前景  .systemBackground
accent            强调色    .systemBlue
success           成功      .systemGreen
danger            错误/危险 .systemRed
边框              .separator.opacity(0.55)（见 PaperCard）
```

结构令牌：

- 卡片圆角 18pt（`PaperCard`），输入区圆角 12–16pt，标签胶囊 `Capsule`。
- 分类色来自后端 `colorHex`，通过 `Color(hex:)` 转换，属于**数据颜色**，不得当作主题色使用。

已有的两个共享修饰符必须复用，不得在各页面另写一套：

- `.paperCard()` — 卡片表面 + 边框
- `.reminderCanvas()` — 隐藏 UIKit 默认 List/Form 画布，改用 `paper` 背景与 `ink` 前景（这是深色模式不出现白色不透明框的前提）

**约束（重要，已踩过坑）：** `accent` 不得跟随 `label`。iPad List 用 tint 作为选中行填充色，深色下 `label` 为白色会与白色标题冲突导致文字消失。

## 3.3 颜色

**必须**

颜色不能成为传达重要状态的唯一方式：

- 逾期：红色圆点/图标 **且** 文字「已逾期」**且** `exclamationmark.circle.fill` 图标。
- 完成：绿色状态点 **且** 文字「已完成」**且** 标题删除线 **且** 标题转为 `muted`。
- 高优先级：红色 **且** 文字「高优先级」。
- 分类：色点 **且** 分类名称（详情页 `DetailLine` 必须给出名称）。

深色模式：

- 新增颜色必须同时检查浅色与深色，优先使用 UIKit 动态语义色而不是固定 hex。
- 不为深色模式复制另一套布局。
- List / Form 新增页面必须调用 `.reminderCanvas()`，否则会回落到浅色不透明画布。

**项目主题支持：**

- [ ] 仅浅色
- [x] 浅色 + 深色
- [x] 跟随系统
- [ ] 手动主题切换（第一版不提供 App 内主题切换）

## 3.4 字体与文字

**项目决定**

主字体：

> iOS 系统字体（SF Pro）。唯一例外是详情页标题使用 `.system(size: 31, weight: .semibold, design: .rounded)`。不引入自定义字体。

正文基础字号：

> SwiftUI `.body`（默认 17pt），随 Dynamic Type 缩放。副信息用 `.subheadline` / `.caption`，最小不低于 `.caption2`（Widget 内除外）。

其他要求：

- 界面文字必须为简体中文，包括 Widget 与本地通知文案。
- 核心操作、状态与错误不得只依赖长按或次级菜单才能理解。
- 选中态不得通过改变字重造成文字宽度变化。

---

# 4. 页面与布局架构

## 4.1 主要布局

横屏 / 宽屏主要布局：

> `NavigationSplitView` 三栏。侧栏 `min 240 / ideal 260 / max 300`，中栏 `min 360 / ideal 460 / max 560`，详情栏 `min 380 / ideal 480 / max 620`。

竖屏 / 窄屏主要布局：

> 侧栏自动收起为左上入口，保持「列表 + 详情」双栏；空间进一步不足时退化为导航堆栈：视图 → 列表 → 详情。

主要固定区域：

- 侧栏底部的连接状态条与设置按钮（`safeAreaInset(edge: .bottom)`，`.bar` 背景）
- 中栏顶部的标题 + AI + 新建操作条（`safeAreaInset(edge: .top)`，固定 62pt）
- sheet 的导航栏与确认 / 取消按钮位置

主要可变化区域：

- 中栏的分组列表
- 详情栏的内容卡片
- AI 工作区的草稿区

布局不得依赖设备名称判断，应依据 `NavigationSplitView` 的实际可用宽度与尺寸类。

**横屏是主形态。** 界面验收以横屏三栏为准，竖屏是降级形态；两者都要能用，但取舍时先保证横屏。

## 4.2 页面宽度

**项目决定**

本项目采用：

- [x] 全屏应用布局（三栏铺满窗口）
- [ ] 居中最大宽度
- [x] 混合布局（栏内内容限宽）
- [ ] 其他

最大内容宽度：

> 详情栏内容 `maxWidth: 560`；AI 工作区内容 `maxWidth: 680`；AI 草稿字段值区域 `maxWidth: 290`。外层栏本身铺满。

同时必须支持 iPadOS 多任务分屏与 Slide Over 下的窄宽度，不得假设永远是全屏。

## 4.3 主界面与次要界面

核心任务（看 / 完成 / 创建）占据中栏与详情栏。设置、连接、令牌、通知授权、演示工作区切换一律位于设置 sheet，不得常驻主界面。

---

# 5. 响应式设计

## 5.1 原则

**必须**

响应式不是把横屏界面缩小。空间不足时按以下顺序重新组织：

1. 信息优先级（见 2.4）；
2. 当前任务（正在浏览 / 正在创建）；
3. 输入方式（触控、外接键盘、Apple Pencil、指针）；
4. 内容需要。

## 5.2 项目断点

本项目以 `NavigationSplitView` 的可用宽度与 SwiftUI 尺寸类划分，不以设备型号判断。

### 宽屏

条件：

> 常规宽度且宽度足以同时容纳三栏（11 英寸 iPad Pro 横屏全屏、大尺寸分屏左侧）。

布局：

> 侧栏 + 列表 + 详情三栏全部可见，侧栏常驻展开。

### 中等空间

条件：

> 宽度不足以容纳三栏，但可容纳两栏（iPad 竖屏全屏、1/2 分屏）。

布局：

> 侧栏自动收起为工具栏入口（以 overlay 方式覆盖，不挤压列表），列表 + 详情双栏保持稳定。

### 窄屏

条件：

> 紧凑宽度（Slide Over、小比例分屏）。

布局：

> 退化为导航堆栈：视图列表 → Deadline 列表 → 详情。顶部保持紧凑，创建与 AI 入口保持在列表页顶部操作条内。

## 5.3 窄屏要求

**必须**

- 不得出现意外的横向滚动；长标题截断或换行，不得横向撑开。
- 主要操作（完成、新建、AI、返回）在任何宽度下都必须可达。
- 不得仅依靠指针 hover 或长按暴露必要功能：`contextMenu` 中的「完成 / 重新打开」必须在详情页有等价的常规按钮（当前已满足）。
- 文案缩短后仍需保持含义清楚，避免只剩图标且无 accessibility label。
- 隐藏内容按信息优先级处理：先隐藏同步说明与分类色点辅助信息，再隐藏标签，最后才是截止时间。截止时间与标题不得被隐藏。

---

# 6. 滚动与 Overflow

## 6.1 滚动责任

**必须**

每个区域明确「谁负责滚动」：

- 侧栏：`List` 自身滚动，底部状态条固定不随列表滚动。
- 中栏：分组 `List` 自身滚动，顶部操作条固定不随列表滚动。
- 详情栏：外层 `ScrollView` 滚动；内部卡片不再单独滚动。
- sheet：AI 工作区外层 `ScrollView` 滚动；创建 / 设置由 `Form` 滚动。

避免：

- 详情卡片与详情 `ScrollView` 同时争夺滚动；
- 加载态与内容态之间滚动容器发生变化（Loading 的 `ProgressView` 不得替换掉整个栏的容器结构）；
- 切换筛选后滚动位置无故归零（新筛选下的位置重置是预期，同一筛选下的刷新不得重置）。

## 6.2 稳定滚动区域

SwiftUI/iPadOS 使用覆盖式滚动指示器，不存在 Web 的滚动条占位问题。等价要求：

- 每个可滚动容器必须有明确的最小尺寸，Loading / Empty / Error 状态之间容器尺寸保持一致（当前用 `frame(maxWidth: .infinity, maxHeight: .infinity)` 承载 `ProgressView`）。
- 下拉刷新（`.refreshable`）只出现在中栏列表，且不得在刷新过程中改变列表容器高度。

## 6.3 横向内容

宽表格、长代码、长 URL 等无法压缩的内容由自身容器承担横向滚动，不得让整栏产生横向滚动。当前界面没有此类内容；如未来加入，必须遵守本条。

---

# 7. 状态设计

每个异步或数据驱动区域都必须考虑适用状态。本项目至少检查：

- Loading
- Empty
- Success / Content
- Error
- Disabled
- Stale / Offline

已定义的状态来源：`DeadlineStore` 的 `isLoading`、`errorMessage`、`syncNote`、`isDemoMode`。

**并发的刷新必须只有最后一次算数。** `refresh()` 每次领一个自增的代数号，
返回时号对不上就整趟丢弃。冷启动的演示刷新和 `.task` 里恢复真实连接后的刷新会并发跑，
没有这个守卫时先发起、后返回的那个会把真实数据覆盖成演示数据——而且此时 `isDemoMode`
已经是 `false`，演示数据还会被写进真实的离线缓存。刷新过程中仓库可能被换掉（连接 / 断开），
所以整趟只认开始时取到的那个仓库。

**创建成功后必须跟刷新走同一条目录回填路径。** API 返回的 DTO 里 category id 是占位的
`"uncatalogued"`、subject 只有 id 没有名字；`create()` 若直接把返回值插进列表，
新建的这条会立刻从分类筛选里消失、学科名是空的，要等下一次 refresh 才恢复。

**演示仓库不得人造延迟。** `MockDeadlineRepository` 的读写一律立即返回。
人为 sleep 只会让演示工作区显得比真实后端还慢，不产生任何信息。

## 7.1 Loading

- 仅当「正在加载且当前没有任何数据」时才显示整栏 `ProgressView`；已有数据（含离线缓存）时必须继续显示旧数据，只通过 `syncNote` 表达正在同步。
- 整栏 `ProgressView` **延迟 250ms 才出现**（`DelayedProgressView`）。冷启动时演示 / 缓存数据约 100ms 就到位，
  不延迟的话会闪一个转圈再跳成内容，读起来比直接出内容还慢；真的慢时反馈照常出现。
- Loading 不得清空整个界面、坍缩布局或让已加载的无关数据闪烁。
- Loading 不得阻止与该请求无关的操作（例如仍可打开设置、仍可浏览已有详情）。
- AI 解析期间只禁用「分析这段话」按钮，不阻塞整个 sheet；按钮文案变为「正在分析…」，位置不变。

## 7.2 Empty

空态必须说明场景，不得只显示「无数据」。当前实现：

- 列表为空：`ContentUnavailableView("这里没有截止事项", …, description: "没有符合当前视图的截止事项。")`
- 未选中详情：「选择一个截止事项 / 详细信息会显示在这里。」
- Widget 无未来事项：「未来暂无截止事项」

新增空态时必须回答：为什么为空、用户可以做什么、这是正常还是异常。

## 7.3 Error

错误信息优先告诉用户：

1. 什么操作失败（「无法更新截止事项」「连接失败」）；
2. 当前数据是否仍然安全（乐观更新失败已回滚，用户输入仍保留）；
3. 接下来能做什么（重试、检查地址与令牌、继续使用演示工作区）。

分级呈现：

- **有数据 + 刷新失败** → 不弹错误，降级为 `syncNote`：「离线 · 正在显示已保存的截止事项」。
- **无数据 + 刷新失败** → `errorMessage`，通过 alert 呈现。
- **写入失败（创建 / 完成 / 重开）** → 回滚本地状态 + alert，表单内容保留。
- **连接失败** → 在设置 sheet 内的 `connectionMessage` 就地显示，用 `danger` 色，不弹全局 alert。
  文案必须说明**地址与令牌都没有保存、当前仍在演示工作区**，而不是只抛系统原文。

**连接成功的判据必须是一次真实请求。** `DeadlineStore.connect(to:)` 自己调 `fetchDeadlines()`
并让错误抛出；**不得**拿 `refresh()` 的结果判断连通性——本地一旦已有数据（冷启动的演示数据或离线缓存），
`refresh()` 会把失败降级成「离线」提示并清掉 `errorMessage`，于是连不上被当成连上了，
错误的令牌还会被写进钥匙串。启动时恢复已保存的连接走 `restoreConnection(_:)`，
那份配置在保存时已经探测过，不再探测，直接交给 `refresh()` 按离线规则降级。

界面不得暴露：stack trace、数据库字段、后端内部字段名、原始 HTTP 响应体、令牌内容、无意义错误码。

## 7.4 Offline / Stale

- 冷启动先展示 SwiftData 离线缓存，`syncNote` 显示「正在显示已保存的截止事项」。
- 网络恢复并同步成功后改为「已同步」；演示模式显示「演示工作区」。
- 演示模式必须始终可识别：侧栏底部状态条文案为「演示工作区」而不是「已连接 Calendar」。

---

# 8. 表单与输入

## 8.1 基础原则

**必须**

创建表单与 AI 草稿必须明确处理：

- 默认值：截止时间默认为明天此刻（`DeadlineDraft`），分类默认取目录第一项（若默认分类不在目录中，`onAppear` 时替换）。
- 必填与选填：标题必填；备注、标签、学科选填。
- 校验：标题去空白后非空才允许提交；分类必须来自后端目录（且未归档）；学科仅在分类 `kind == "academics"` 时出现、必须属于该分类且 `active = 1`；
  **标签最多 5 个**（后端硬约束，见 `Calendar/API_DOC.md` §Tags），达到上限后其余标签按钮必须禁用，不得等到提交才由后端报 400。
- 提交中：按钮文案变为「正在创建…」并禁用。
- 提交失败：sheet 不关闭，字段全部保留，错误通过 alert 提示。
- 成功后的行为：关闭 sheet，新建项插入列表并自动选中。

## 8.2 校验

- 使用应用自己的校验反馈（禁用提交按钮、就地提示），不依赖系统弹窗做字段校验。
- 错误尽可能显示在相关字段附近；连接错误显示在连接 Section 内。
- 错误出现时不得改变主要按钮位置（工具栏确认按钮位置固定）。
- 分类切换后如果已选学科不属于新分类，必须自动清空学科（`onChange(of:)`），不得提交非法组合。

## 8.3 提交

- 提交开始必须立即给出反馈（文案 + 禁用）。
- 必须防止重复提交：`isSaving` / `isCreating` 期间按钮禁用。
- 不得锁死与该请求无关的界面。
- 提交失败保留已填内容，不要求用户重新输入。

## 8.4 完成 / 重开的乐观更新

- 点击「完成」立即在界面显示已完成，并取消对应本地通知。
- 随后调用 API，以服务端返回的 Deadline 为最终结果。
- 失败则整体回滚到操作前的 `deadlines` 快照并提示。
- 重开时根据最新截止时间重新排程本地通知。

---

# 9. 系统原生控件

> 原模板第 9 节针对浏览器 `alert()` / `confirm()` / `prompt()` 与 `title` tooltip。SwiftUI 无对应 API，改写为以下 iPadOS 规则。

## 9.1 禁止

**必须**

不得使用 UIKit 直接弹出的 `UIAlertController`、不得绕过 SwiftUI 的状态驱动去 presentViewController。所有提示统一走：

- `.alert(...)`（需要用户确认后才能继续的错误）
- sheet 内就地提示（表单与连接错误）
- 列表内联状态（`syncNote`、状态点与状态文字）

## 9.2 提示与长按菜单

- 不得把只有长按 `contextMenu` 才能看到的必要信息或唯一操作放进菜单；`contextMenu` 只作为已有操作的快捷方式。
- 图标按钮必须有 `accessibilityLabel`，不得用视觉提示代替可读名称。

## 9.3 原生表单控件

本项目规则：

> 一律使用 SwiftUI 原生 `Form`、`Section`、`Picker`、`DatePicker`、`Toggle`、`TextField`、`SecureField`、`TextEditor`、`List`、`ContentUnavailableView`、`ProgressView`，并统一套用 `.reminderCanvas()`。
> 仅在系统控件确实无法表达时才自定义，目前的例外是标签选择：`TagPicker` + 自定义 `FlowLayout` 胶囊按钮。
> 自定义按钮位于 `Form` 行内时必须显式指定 `.buttonStyle(.borderless)`，否则 `Form` 会把同一行的多个按钮合并成行级操作（已发生过：点单个标签会切换全部标签）。

同一产品内不得混用多套明显不同的交互体系。新增控件优先复用已有组件（见第 14 节）。

---

# 10. 弹窗、浮层与临时界面

## 10.1 Sheet（对应 Modal）

Sheet 用于需要暂时离开当前上下文、完成一个明确小任务的场景。本项目共三个：创建、AI 创建、设置。

- 创建 / AI 使用 `.presentationDetents([.large])`；设置使用 `[.medium, .large]`。
- 打开后焦点进入 sheet，背景不可误操作。
- 关闭后焦点回到主界面合理位置（通常是新建或更新后的 Deadline）。
- 必须提供明确的取消 / 关闭与确认按钮，位置固定在导航栏。
- 不得把完整的多步骤页面硬塞进 sheet。
- 同一个 sheet 在不同状态（未解析 / 已解析 / 提交中）之间保持整体尺寸稳定。

## 10.2 Popover / Menu

- 临时浮层不得推动主界面布局。
- 需要处理屏幕边缘、滚动、点击外部关闭、键盘操作与触控行为。
- 竖屏收起的侧栏以覆盖方式出现，不挤压列表与详情。

## 10.3 轻量反馈（Toast）

当前版本没有 Toast 组件。成功反馈通过界面本身的即时变化表达（新建项出现并被选中、行状态立即改变）。

如未来加入 Toast，只可用于：操作成功、非阻塞错误、可撤销操作、后台状态。不得承载用户必须阅读后才能继续的重要决策。

---

# 11. 交互反馈

## 11.1 即时反馈

**必须**

用户操作后界面应尽快确认「操作已被接收」：

- 完成 / 重开：乐观更新，立即改变状态点、文字与删除线。
- 创建：按钮文案 + 禁用 → 成功后新项出现并选中。
- AI 解析：按钮文案变为「正在分析…」，草稿区解析完成后出现。
- 刷新：`.refreshable` 系统指示器 + `syncNote`。

## 11.2 交互状态

控件应根据输入方式提供合理状态：按下态、选中态、禁用态、键盘焦点态。

- 不得通过改变元素几何尺寸来表达普通选中态或按下态。
- 列表选中依赖 `List` 的 tint 填充；深色下 tint 必须是 `systemBlue` 而不是 `label`（见 3.2）。
- 禁用态必须同时有视觉与可用性变化，不能只靠变淡。

## 11.3 破坏性操作

- 「断开并使用演示工作区」使用 `role: .destructive`，因为它会切换数据源并停止写入远端。
- 完成 / 重开不是破坏性操作，不得弹确认框，应保持一步完成。
- 第一版不提供删除 Deadline；若后续加入，必须提供二次确认或撤销入口之一。

---

# 12. 动效

## 12.1 原则

动效用于解释状态变化、表示空间关系、提供交互反馈、减少突兀。

本项目动效必须短促、自然、可中断，使用 SwiftUI 默认过渡与系统导航动画。

不使用：弹跳、位移式 hover、缩放式 hover、持续吸引注意的动画、延迟用户完成操作的动画。

选中态不得通过改变字重造成文字宽度变化。

## 12.2 减少动态效果

**必须**

涉及位移、缩放、脉动、连续动画时，必须遵循系统「减少动态效果」：

```swift
@Environment(\.accessibilityReduceMotion) private var reduceMotion
```

- 不得使用持续动画传达唯一信息（加载必须同时有文案或明确指示器）。
- 减少动态效果开启时，过渡降级为淡入淡出或直接切换，不得保留位移动画。

---

# 13. 导航

## 13.1 导航原则

用户应能理解自己在哪里、可以去哪里、返回会发生什么。

- 中栏顶部始终显示当前视图名称（`selectedFilterTitle()`），分类 / 学科 / 标签显示其真实名称而不是通用字样。
- 同一层级导航保持一致：侧栏所有视图项统一使用 `Label` + SF Symbol。
- 竖屏返回按钮行为遵循 `NavigationSplitView` 默认语义。

## 13.2 状态保留

在同一任务内切换视图、sheet、详情时需要保留：

> 当前筛选、当前选中的 Deadline（若在新视图中仍可见）、列表滚动位置、AI 工作区已输入文本与已解析草稿、创建表单未提交内容、通知与 Widget 深链接的待选中目标 `pendingDeadlineID`。

## 13.3 深链接

- URL Scheme：`ai0506reminders://deadline/<id>`（见 `Shared/RemindersRoute.swift`）。
- 来源：Widget `widgetURL`、本地通知点击。
- 行为：切换到「全部截止事项」并选中目标；数据未加载完成时记录 `pendingDeadlineID`，刷新后补选。
- 深链接不得关闭用户已打开的 sheet 中未保存的内容而不作提示。

---

# 14. 组件与复用

## 14.1 优先复用

**必须**

新增 UI 前先检查是否已有相同或相似组件、相同交互模式、已有令牌。当前已有的共享组件：

```text
RemindersTheme        颜色令牌
PaperCard / .paperCard()      卡片表面
.reminderCanvas()             List/Form 深色安全画布
DeadlineRow                   列表行
DetailLine                    详情键值行
TagPicker                     标签多选
FlowLayout                    自适应换行布局
DraftField                    AI 草稿字段行
ContentUnavailableView        空态（系统）
```

不得因为新页面方便就重新造第二套卡片、标签、空态或字段行。

## 14.2 新组件

创建新组件前判断：是否确实存在新的交互需求、是否可由已有组件组合、是否只是视觉差异、是否真的会被复用。

不要为每个很小的视图结构单独抽象。

## 14.3 App 与 Widget 共享

Widget target 复用 `Reminders/Models` 与 `Reminders/Theme`，通过 `Shared/WidgetDeadlineSnapshot.swift` 交换轻量模型。

- Widget 不得引入独立的第二套颜色与样式。
- 快照结构变更必须升级版本号（当前为 v2），避免旧缓存被错误解读。

---

# 15. 实时更新与后台刷新

## 15.1 后台更新

**必须**

`refresh()`、Widget 刷新、通知重排不得：

- 覆盖正在输入的内容；
- 关闭用户打开的 sheet；
- 清除当前筛选；
- 重置当前选中项（若仍可见）；
- 抢走键盘焦点；
- 无故改变滚动位置。

## 15.2 数据一致性

- 本项目没有 WebSocket / 推送，刷新为拉取式：冷启动、手动下拉、连接设置变更后触发。
- 本地缓存（SwiftData + App Group `UserDefaults`）只是展示加速与离线兜底，不是业务真相来源。
- 写入结果一律以服务端返回为准；乐观更新失败必须回滚。
- 分类 / 学科 / 标签目录以后端 `fetchCatalog()` 为准，界面不得写死可选项（`DeadlineCategory.all` 仅用于演示与兜底）。

---

# 16. 可访问性

## 16.1 基础要求

**必须**

优先使用语义正确的 SwiftUI 控件：

```text
按钮   → Button
列表   → List
表单   → Form / Section
选择   → Picker
开关   → Toggle
空态   → ContentUnavailableView
```

不要用可点击的容器模拟原生控件。

## 16.2 键盘与指针

iPad 需考虑妙控键盘、外接键盘与触控板：

- Tab 顺序合理；
- 焦点可见；
- Return 提交表单、Esc 关闭 sheet；
- sheet 打开时焦点被正确捕获；
- Picker、Toggle、DatePicker 可键盘操作。

## 16.3 Accessible Name

仅有图标的按钮必须有可读名称。当前已设置：

```text
gearshape → "提醒事项设置"
sparkles  → "用 AI 创建截止事项"
plus      → "新建截止事项"
```

新增图标按钮必须同步添加 `accessibilityLabel`。状态色点必须有伴随文字，否则需要补充可访问描述。

## 16.4 Dynamic Type

- 正文使用系统字体样式，必须随 Dynamic Type 缩放。
- 放大字号后不得出现关键信息被裁切或按钮无法点击；固定高度的槽位（62pt 操作条、52pt 草稿字段行、38pt 占位）在最大字号下需复核。

## 16.5 触控

本项目最低触控尺寸：

> 44×44pt。当前 AI / 新建圆形按钮的可点区域为 34pt，需要扩大到 44pt 或增加 `contentShape` 命中区域（见第 22 节技术债）。

---

# 17. 文案

## 17.1 语言

界面、Widget、本地通知一律使用简体中文。不得混入英文按钮词（Today / Complete 等）。技术标识（URL、bundle id、令牌）除外。

## 17.2 按钮

优先使用明确动作：

```text
创建   保存   完成   重新打开   连接 Calendar   重新连接 Calendar   分析这段话   重试
```

避免大量使用含义模糊的「确定」「好的」「是」。（alert 的取消按钮「好」属于已接受的例外。）

## 17.3 错误

错误信息应帮助用户继续完成任务。

避免：

> 操作失败。

优先：

> 无法更新截止事项。你的修改已恢复到之前的状态，可以重新尝试。

设置页已采用的良好范例：

> 访问令牌只保存在 iPad 钥匙串中。本应用仅读取和写入 Calendar Deadline API，不会访问 Apple 日历或 Apple 提醒事项。

## 17.4 内部信息

界面不暴露仅开发者理解的实现细节，也不得显示访问令牌明文、API 路径或后端字段名。

---

# 18. 项目特有规则

## 18.1 Deadline 列表（中栏）

- 按截止日期分组；分组标题为「今天」「明天」或「星期X 月 日」。
- 排序：未完成优先于已完成，其次按截止时间升序；组内按时间升序。
- 每行只显示：分类色点、标题、截止时间、高优先级标记、标签摘要、逾期图标。不得增加更多常驻信息。
- 时间格式：全天显示「8月28日」；今天到期显示「15:00」；其他显示「8月28日 15:00」。
- 已完成行：标题加删除线、转 `muted`、色点 `opacity(0.35)`。
- 每行提供 `contextMenu` 的「完成 / 重新打开」，但该操作必须在详情页有等价按钮。
- 「今天」「即将到期」「已逾期」视图只显示未完成项；「全部」「分类」「学科」「标签」视图显示全部状态。

## 18.2 Deadline 详情（右栏）

- 顺序固定：状态标记 + 主操作 → 标题 → 属性卡片 → 备注卡片。
- 主操作按钮（完成 / 重新打开）固定在右上，不随内容长度移动。
- 属性卡片顺序固定：截止 → 分类 →（学科）→ 优先级 →（标签）。
- 学科行仅在该 Deadline 有学科时出现；标签行仅在有标签时出现。
- 未选中时显示空态，不得显示空白。

## 18.3 侧栏

- 顺序固定：今天 / 即将到期 / 已逾期 / 全部 → 分类 → 学科 → 标签。
- 学科与标签 Section 在目录为空时整体隐藏，不显示空 Section。
- 分类使用真实分类色点；学科使用 `book.closed`；标签使用 `tag`。
- 底部状态条固定：连接状态点 + 文案 + 设置按钮。

## 18.4 学科（Academics Subject）

- 学科只在 `category.kind == "academics"` 的分类下出现，创建表单与 AI 草稿都必须遵守；普通分类下 `subject_id` 必须为空，否则后端返回 400。
- 切换分类后必须清空不匹配的学科。
- Calendar 的 Course（课程）层不进入 Reminders，不得在界面中出现。

## 18.5 AI 创建

**三步流程，一次只占一屏。** 输入原文、等待解析、确认草稿是三件不同的事，不得挤在同一屏里
（早期版本把草稿直接追加在输入框下方，导致解析完成时页面突然变长，且用户分不清「我写的」和「AI 写的」）。

| 步骤 | 标题 | 内容 | 主操作 |
|---|---|---|---|
| 1 输入 | AI 创建 | 说明文字、原文输入框、本地解析标注 | 分析这段话 |
| 2 分析中 | 正在分析 | 呼吸指示器、状态文字、原文回显 | 取消 |
| 3 草稿 | 确认草稿 | 原文卡片（含「修改原文」）、可编辑草稿 | 创建截止事项 |

规则：

- 必须明确标注「本地演示解析 · 不会发送 AI 请求」，第 1、2 步都要出现，不得让用户误以为调用了真实 AI。
- AI 结果**不得**未经用户确认直接写入。
- 草稿的全部字段可编辑：标题、分类、学科、截止、全天、优先级、标签、备注。
- 显示解析置信度，但置信度不得作为是否允许创建的门槛。
- **第 3 步必须同屏显示原文**——草稿是对原文的解读，用户要能当场核对。
- 「修改原文」退回第 1 步，**输入框里的文字原样保留**（§2.3）。
- 第 2 步必须可取消，取消退回第 1 步并保留原文，不得只能等它跑完。
- sheet 在三步之间**整体尺寸不变**（`.presentationDetents([.large])`，内容统一 `maxWidth: 680`）；
  只有导航标题随步骤变化，那是有意的步骤指示（§13.1）。
- **第 2 步只在解析超过 180ms 时才出现，且不得有最短停留时间。** 本地演示解析是纯计算、
  瞬间返回，为它切一屏再切回来，用户读到的不是进度而是一次多余的转场；
  早期版本在这里加过 0.45 秒的补偿等待，结果正是那段等待让「AI 创建」显得卡住。
  这 180ms 内输入屏留在原地，按钮文案变「正在分析…」并禁用（§7.1）表示操作已被接收。
  真实 AI 的请求本来就超过这个阈值，第 2 步会自然回来，三步结构不重做。
- **第 3 步的「创建截止事项」常驻在底部操作条（`safeAreaInset(edge: .bottom)`），不随草稿内容滚动。**
  草稿字段比一屏长，跟着内容滚的话横屏下要先滚到底才看得见主操作，读起来像这一步没有出口（§4 主要操作必须可达）。
- 动画不得是状态的唯一来源：呼吸指示器旁必须有「正在分析这段话…」文字（§12.2）。
- 呼吸指示器只改透明度，不做位移与缩放；开启「减少动态效果」时退化为静态圆点（§12.2）。
- 未来接入真实 AI 时只替换解析器与 API 层，这三步结构不重做；API Key 不得进入 iPad App。

## 18.6 Widget

- 只显示「未来且未完成」的 Deadline。
- medium：取最近三个有事项的日期，各显示一项；同日按 高 / 普通 / 低 优先级再按时间决胜。
- small：显示首项 + 「另有 N 项即将到期」。
- 空态：「未来暂无截止事项」。
- **medium 的每一行各自可点**，用各自的 `Link`。整块共用一个 `widgetURL` 的话，
  点第二、三行也只会跳到第一条。small 整块只有一个点击目标，才用 `widgetURL`。
- **真实 timeline 读不到快照时显示空态，绝不回落到示例数据。** 示例数据只允许出现在
  `placeholder` 和 `context.isPreview` 的预览里——主屏上出现用户从没建过的事项，看起来跟真的一样。
- 表头的条数写实际数量，不写死「最近 3 项」。
- 点击进入对应 Deadline 详情。
- App 数据变化后必须刷新 Widget timeline。

## 18.7 本地通知

- 仅在用户主动在设置中开启后申请权限；被拒绝时开关回退并给出可操作说明。
- 定时事项提前 15 分钟；全天事项当天 09:00；最多排程最近 48 项。
- 创建 / 修改 / 完成 / 重开 / 刷新后必须同步重建通知。
- **重排前按标识前缀清掉全部已排的截止提醒**，不能只清当前列表里那些 id：
  服务端删掉的、或落在本次同步窗口之外的 Deadline 已经不在列表里，它们的通知会留下来
  继续弹，用户看到一条早就不存在的提醒。同理，关闭提醒只清本 App 的前缀，
  不用 `removeAllPendingNotificationRequests()` 一把梭。
- 通知点击进入对应 Deadline 详情。
- 通知只存在于 iPad 本地，不写入 Apple 提醒事项。

## 18.9 与 Calendar 的一致性约束

Reminders 是 AI0506 Calendar 的第二客户端，必须遵守 `Calendar/production/FRONTEND_SPEC.md` 的共同原则，
并与 Web 主日历、macOS 与 Android 客户端保持同一套数据语义。

- **稳定性靠量不靠眼**（Calendar §2.3）：回归时连续切换筛选 / 连续选中不同 Deadline，中栏顶部操作条与侧栏底部状态条的屏幕坐标必须完全不变。
- **Deadline 的主视觉语义是 priority**（Calendar §6）：分类 / 学科色只作为辅助边线或色点，不得盖过优先级与逾期状态。
- **颜色解析顺序**：事项显式 `color` > Subject 颜色 > Category 颜色。`null` / `"default"` 表示「跟随分类或科目」，不得在客户端把当时的分类色快照成事项颜色——否则后端改配色时 iPad 不会跟着变。
- **时区锚点是 `Asia/Shanghai`**：后端的「今天」「逾期」按上海时间计算。界面分组与「今天 / 已逾期」筛选不得直接用设备时区得出与后端不一致的结论。
- **全天 Deadline 的逾期规则**：截止日**当天仍为 `open`**，次日（上海时间）才变 `overdue`。客户端不得把全天项在当天就标红。
- **语言**：后端分类与学科名称是英文（Academics / Research / AI0506 Project / Personal / Tech；Math / Physics / CS / English / Other Subjects）。中文界面直接显示后端原名，不在客户端私自翻译或改写——翻译会让 iPad 与 Web 对同一条数据显示不同名称。需要中文名时应在 Calendar 后端统一改名。
- **假数据两个项目共用一份**：`../Calendar/fixtures/sample-workspace.json` 是唯一权威副本，
  Reminders 用 `Scripts/sync-fixtures.sh` 同步到 `Reminders/Resources/sample-workspace.json`，
  演示工作区直接读它。这样本地起一个真的 Calendar（`npm run db:seed-fake` + `npm run dev`）之后，
  演示模式和真实后端看到的是同一批事项，来回切换界面是连续的。
  分类 / 学科 / 标签的名字与配色以 Calendar 的 **迁移脚本** 为准，不以任何文档为准。
- **数据窗口**：`GET /api/deadlines` 当前只取 `-30 天 ~ +120 天` 且 `include_completed=true`。「全部截止事项」实际是这个窗口，空态文案不得让用户误以为账户里真的没有任何数据。
- **写入契约以 Calendar 为准**：`POST` / `PUT` / `complete` / `reopen` / `DELETE`（软删除）均已在 `Calendar/API_DOC.md` 冻结；complete 与 reopen 幂等，重复调用返回当前对象。

## 18.8 安全与隐私显示规则

- 访问令牌只写入 iPad 钥匙串，仅在连接成功后保存。
- 令牌输入使用 `SecureField`；已保存时用占位文案提示，不得回显明文。
- 令牌不得出现在源码、`project.yml`、README、日志、错误信息或截图中。

---

# 19. AI / 开发实施规则

修改前端前：

1. 阅读本规格；
2. 检查已有组件（14.1 清单）与 `RemindersTheme` 令牌；
3. 检查当前页面已有交互模式；
4. 检查 `REMINDERS_PLAN.md` 中的业务规则与后端契约；
5. 优先沿用已有设计语言。

## 19.1 不要顺手重设计

**必须**

完成一个功能时，不要顺手：

- 重排无关页面；
- 修改无关组件；
- 更换整体视觉风格；
- 重构与任务无关的交互；
- 删除看起来「暂时没用」但属于产品流程的 UI（例如全天模式下的 38pt 占位、`syncNote` 的空格占位、`.buttonStyle(.borderless)`、`accent` 不跟随 `label` 的约束——这些都是修复已知缺陷的产物）。

发现现有设计存在问题应记录到第 22 节并单独处理。

## 19.2 规范与现状冲突

代码现状、本规格与任务要求冲突时，不要自行猜测覆盖顺序。应先明确：当前行为是什么、规范要求什么、新需求要求什么，然后同时修改实现与规范，使最终状态一致。

## 19.3 状态完整性

实现组件时不要只实现 happy path。根据组件性质检查：

```text
默认   按下   选中   禁用   加载中   空   错误   成功   离线
```

不适用的状态可以忽略，但必须是有意识的判断。

## 19.4 深色模式检查清单

新增任何 `List`、`Form`、`ScrollView` 容器时必须：

1. 调用 `.reminderCanvas()`；
2. 为自定义行背景显式指定 `RemindersTheme.card`（`listRowBackground`）；
3. 在深色下确认选中行文字与 tint 填充不冲突；
4. 确认没有出现 UIKit 默认白色不透明画布。

---

# 20. 验收

前端改动完成后至少检查以下内容。

## 20.1 布局

- [ ] iPad 横屏三栏布局正常
- [ ] iPad 竖屏双栏 / 侧栏收起正常
- [ ] 分屏与 Slide Over 下无意外横向滚动、主要操作仍可达
- [ ] 长标题、长备注、大量标签不破坏核心布局
- [ ] Deadline 数量变化不造成无关位移
- [ ] 滚动发生在预期容器

## 20.2 状态

- [ ] Loading（首次加载 / 已有数据刷新）
- [ ] Empty（列表空 / 未选中 / Widget 空）
- [ ] Error（无数据失败 / 写入失败 / 连接失败）
- [ ] Success
- [ ] Disabled（标题为空、提交中）
- [ ] Offline（缓存展示 + `syncNote`）
- [ ] Demo 模式可识别

## 20.3 交互

- [ ] 触控
- [ ] 外接键盘与触控板
- [ ] Apple Pencil
- [ ] 连续快速完成多条
- [ ] 请求失败回滚
- [ ] 重复点击提交不产生重复创建

## 20.4 用户上下文

- [ ] 刷新不覆盖正在输入的内容
- [ ] 刷新不关闭已打开的 sheet
- [ ] 筛选不意外重置
- [ ] 滚动不无故跳动
- [ ] 更新不抢走焦点
- [ ] 深链接进入后仍能正确选中目标
- [ ] AI 从草稿「修改原文」退回后，原文仍在
- [ ] AI 分析中取消后退回输入步骤，原文仍在

## 20.5 主题

- [ ] 浅色模式
- [ ] 深色模式（含所有 List / Form 画布）
- [ ] 深色下列表选中行文字可读
- [ ] 对比度正常

## 20.6 动效与可访问性

- [ ] 动效没有造成布局抖动
- [ ] 「减少动态效果」下表现正常
- [ ] Dynamic Type 放大后无裁切、按钮可点
- [ ] 图标按钮均有 accessibility label

## 20.7 Widget 与通知

- [ ] Widget 只显示未来未完成事项
- [ ] Widget 空态正常
- [ ] Widget 点击进入对应详情
- [ ] 通知开关拒绝授权时正确回退
- [ ] 完成后对应通知被取消

---

# 21. 项目测试命令

前端相关改动完成后运行：

```bash
xcodegen generate
```

```bash
xcodebuild -project AI0506Reminders.xcodeproj -scheme AI0506Reminders -destination 'platform=iOS Simulator,name=iPad Pro 11-inch (M5)' build CODE_SIGNING_ALLOWED=NO
```

```bash
xcodebuild -project AI0506Reminders.xcodeproj -scheme AI0506Reminders -destination 'platform=iOS Simulator,name=iPad Pro 11-inch (M5)' test CODE_SIGNING_ALLOWED=NO
```

当前自动化测试覆盖：中文模拟解析、英文模拟解析、Deadline 深链接路由、Widget 三日期与同日高优先级选取。界面布局与深浅色仍需在 iPad Pro 11-inch 模拟器手动复核。

---

# 22. 已知例外与技术债

| 位置 | 问题 | 原因 | 状态 |
|---|---|---|---|
| `ContentView.swift` 中栏顶部 AI / 新建按钮 | 可点区域 34×34pt，低于 16.5 的 44×44pt 最低触控尺寸 | 为保持 62pt 操作条的紧凑视觉 | 待处理 |
| `ContentView.swift` 详情页 | 缺少「编辑」与「删除」入口，`REMINDERS_PLAN.md` §5 列为第一版候选 | 产品尚未决定第一版是否提供；后端 `PUT` / `DELETE` 契约**已在 `Calendar/API_DOC.md` 冻结**，不构成阻塞 | 待处理 |
| 全局错误呈现 | 写入失败统一走 alert，没有非阻塞 Toast 与撤销入口 | 第一版未建立 Toast 组件 | 待处理 |
| `DeadlineStore.refresh()` | 刷新时整体替换 `deadlines`，未按数据指纹判断是否需要重绘 | 当前数据量小，未观察到卡顿 | 待处理（500 / 1000 条性能测试前需复核） |
| `DeadlineStore.applyCatalog` | 分类按名称大小写不敏感匹配，而不是按 id | `CalendarAPIRepository.DeadlineDTO.model` 把每条 Deadline 的 category id 硬编码为 `"uncatalogued"`，名称匹配是唯一的补救路径 | 待处理 |
| `CalendarAPIRepository` + 侧栏分类筛选 | 分类已归档（如迁移 0012 归档的 Math / Physics / CS）时，`GET /api/categories` 不返回它，`applyCatalog` 匹配不上，该 Deadline 的 category id 停留在 `"uncatalogued"`，在任何分类筛选下都不可见 | 目录只取未归档分类，历史数据仍引用归档分类 | 待处理 |
| `CalendarAPIRepository` 学科解析 | 学科 `active = 0` 时 `GET /api/subjects` 不返回，`applyCatalog` 查不到，详情页与筛选中该学科静默消失（DTO 占位 name 为空字符串） | 目录只取启用中的学科 | 待处理 |
| `Deadline.isOverdue` | 全天 Deadline 在截止日当天 00:00 后即被判为逾期并标红，与后端「当天仍为 open、次日按 Asia/Shanghai 转 overdue」的规则冲突 | 本地用 `dueDate < Date()` 计算，未区分全天 | 待处理 |
| `DeadlineStore.groups` / 今天·逾期筛选 | 使用设备时区 `Calendar.current` 分组，设备不在上海时区时与后端「今天」不一致 | 未固定时区锚点 | 待处理 |
| `TagPicker` | 没有 5 个标签的上限，超过后要等提交才由后端返回 400 | 客户端未实现后端硬约束 | 待处理 |
| 减少动态效果 | 尚未在代码中显式读取 `accessibilityReduceMotion` | 目前仅使用系统默认过渡 | 待处理 |
| Widget | 只有 Today（未来事项）Widget，规划中的 Upcoming / Overdue Widget 未实现 | 分阶段实施，Phase 5 未完成 | 待处理 |
| AI 创建 | 为本地模拟解析，非真实 AI | 真实 AI 需后端 Proxy，API Key 不得进入 iPad App（Phase 6） | 已接受（第一版） |
| 真机验收 | 通知授权、Widget 添加到主屏、Dynamic Type、Apple Pencil、性能均未在 11 英寸 iPad Pro 真机验收 | 缺少真机与真实 Calendar 凭据 | 待处理 |

不要因为存在旧问题，就默认允许新增相同问题。

---

# 23. 规范优先级

多份文档冲突时按以下顺序处理：

1. 安全、权限与数据完整性要求（令牌只入钥匙串、不使用 EventKit、写入以服务端为准）
2. Calendar 后端接口契约与 Deadline 业务规则
3. 明确的当前产品需求
4. `Calendar/production/FRONTEND_SPEC.md` 的跨客户端共同原则（稳定性、信息层级、颜色语义）
5. 本前端界面规格
6. `REMINDERS_PLAN.md` 的规划描述
7. 已有实现
